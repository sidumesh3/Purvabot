[theme]
primaryColor = "#F57C00"              # Saffron (unchanged)
backgroundColor = "#FFF8E1"           # Ivory (unchanged)
secondaryBackgroundColor = "#F5E2D0"  # Light brownish cream (so input box contrasts better)
textColor = "#262730"                 # Dark slate (unchanged)
font = "sans serif"
